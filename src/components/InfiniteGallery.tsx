import { galleryItems } from '../data/galleryItems';
export function InfiniteGallery() { const items = [...galleryItems, ...galleryItems]; return <div className="gallery-mask mt-16 overflow-x-auto md:overflow-hidden" aria-label="Beauty Portfolio Slider"><div className="gallery-track flex w-max gap-5 px-5 will-change-transform md:gap-7 md:px-10">{items.map((item, i) => <figure key={`${item.src}-${i}`} className="group relative h-[420px] w-[280px] shrink-0 overflow-hidden rounded-[1.35rem] bg-beige/30 md:h-[560px] md:w-[390px] even:mt-10"><img src={item.src} alt={item.alt} width={item.width} height={item.height} loading="lazy" className="h-full w-full object-cover transition duration-700 group-hover:scale-[1.04] group-hover:brightness-105"/><figcaption className="absolute bottom-0 left-0 right-0 translate-y-3 bg-gradient-to-t from-ink/55 to-transparent p-5 text-xs font-bold uppercase tracking-[.22em] text-warm-white opacity-0 transition duration-500 group-hover:translate-y-0 group-hover:opacity-100">{item.label}</figcaption></figure>)}</div></div>; }
