import { motion } from 'framer-motion';
import { ChevronDown } from 'lucide-react';
import { siteConfig } from '../config/siteConfig';
import { AnimatedButton } from './AnimatedButton';
import { HeroHeadline } from './HeroHeadline';
import { HeroVideo } from './HeroVideo';
export function HeroSection() { return <section id="home" className="relative isolate flex min-h-[100svh] overflow-hidden px-5 pb-[clamp(3rem,7svh,6rem)] pt-28 md:px-10 lg:pt-32"><HeroVideo/><div className="relative z-10 mx-auto grid w-full max-w-[1800px] items-end gap-8 self-stretch md:gap-10 lg:grid-cols-[minmax(0,1.18fr)_minmax(18rem,.82fr)]"><div className="flex min-w-0 items-end pb-2 md:pb-5 lg:pb-[clamp(1.5rem,6svh,5rem)]"><HeroHeadline/></div><motion.div className="flex w-full max-w-sm flex-col items-stretch gap-4 justify-self-start pb-1 md:max-w-md lg:justify-self-end lg:self-end lg:pb-[clamp(3rem,16svh,10rem)]" initial={false} animate={{ opacity: 1, x: 0 }} transition={{ duration: .7, delay: .2 }}><AnimatedButton href={siteConfig.bookingUrl}>Online Termin buchen</AnimatedButton><AnimatedButton href="#contact" variant="secondary">Kontaktiere mich</AnimatedButton></motion.div></div><a href="#services" className="absolute bottom-6 left-1/2 z-10 hidden -translate-x-1/2 items-center gap-3 text-xs uppercase tracking-[.35em] text-warm-white/75 md:flex">Explore <ChevronDown className="size-4 animate-bounce-subtle"/></a></section>; }
